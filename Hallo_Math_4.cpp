#include <iostream>
#include <cmath>
/*
一些基础常用的数学相关代码
部分需要导入<cmath>包
*/

int main(){
    double x = 4 ;
    double y = 5 ;
    double z ;

    // z = std::min(x,y); //最小值为什么？X
    // z = std::max(x,y); //最大值为什么？Y

    // z = pow(x,y); // X的Y次方是什么？
    // z = sqrt(x); // X的开方是多少？
    // z = abs(-x); // -X的绝对值是多少？

    // z = round (x); //x四舍五入后是多少？
    // z = ceil (x); //x向上取整后是多少？
    // z = floor (x); //x向下取整后是多少？

    std::cout << z;

    return 0;
}
