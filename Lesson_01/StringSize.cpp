#include <iostream>
#include <string>

int main() {
    std::string str = "Hello, World!";// 字符串初始化
    std::cout << "The size of the string is: " << str.size() << std::endl; // 输出字符串长度
    return 0;
}